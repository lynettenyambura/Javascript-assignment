const numberInRange = (number, rangeStart, rangeEnd) => {
    return (number >= rangeStart && number <= rangeEnd);
  };
  
  // Example usage
  console.log(numberInRange(60, 50, 100)); // Output: true
  